---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 90m0s
  path: ./cluster/flux-system
  sourceRef:
    kind: GitRepository
    name: home-cluster
  prune: false
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
