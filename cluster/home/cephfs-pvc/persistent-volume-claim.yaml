apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: home-shared-pvc
  labels:
    excluded_from_alerts: "true"
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 30Gi
  storageClassName: ceph-filesystem
