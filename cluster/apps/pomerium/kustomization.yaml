---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: pomerium
resources:
  - namespace.yaml
  - certs/internal-ca-tls.enc.yaml
  - certs/internal-tls.yaml
  - certs/certificate.yaml
  - pomerium-issuer.yaml
  - helm-release.yaml
secretGenerator:
  - name: idp-values
    files:
      - values.yaml=idp-values.sops.yaml
configurations:
  - namereference.yaml
