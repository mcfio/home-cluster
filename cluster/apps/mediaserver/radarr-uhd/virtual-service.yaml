---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: radarr-uhd
  namespace: mediaserver
spec:
  hosts:
    - radarr-uhd.${INTERNAL_DOMAIN}
  gateways:
    - istio-ingress/cluster-ingress-gateway
  http:
    - route:
        - destination:
            port:
              number: 7878
            host: radarr-uhd.mediaserver.svc.cluster.local
