---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: &app plex
  namespace: mediaserver
spec:
  interval: 12h
  maxHistory: 2
  chart:
    spec:
      chart: kah-common-chart
      version: 1.1.2
      sourceRef:
        kind: HelmRepository
        name: k8s-at-home
        namespace: flux-system
  upgrade:
    remediation:
      remediateLastFailure: true
  values:
    global:
      nameOverride: *app
    image:
      repository: ghcr.io/k8s-at-home/plex
      tag: v1.27.1.5916-6b0e31a64
    env:
      TZ: Canada/Eastern
      ADVERTISE_IP: "https://plex.${INTERNAL_DOMAIN},https://${VIP_PLEX_SERVICE}:32400"
    service:
      main:
        type: LoadBalancer
        loadBalancerIP: ${VIP_PLEX_SERVICE}
        externalTrafficPolicy: Local
        ports:
          http:
            port: 32400
    ingress:
      main:
        enabled: true
        ingressClassName: nginx
        annotations:
          nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
        hosts:
          - host: &host "{{ .Release.Name }}.${INTERNAL_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - *host
    podSecurityContext:
      supplementalGroups:
        - 44
        - 109
        - 1000
    persistence:
      config:
        enabled: true
        existingClaim: mediaserver-shared-pvc
        subPath: plex
      media-path:
        enabled: true
        type: emptyDir
        mountPath: /data
        readOnly: true
        mountPropagation: HostToContainer
      cache:
        enabled: true
        type: pvc
        existingClaim: rclone-cache
      transcode:
        enabled: true
        type: emptyDir
    resources:
      requests:
        cpu: 500m
        memory: 1Gi
        gpu.intel.com/i915: 1
      limits:
        cpu: 1000m
        memory: 2Gi
        gpu.intel.com/i915: 1
    additionalContainers:
      rclone:
        name: rclone
        image: rclone/rclone:1.58.1
        args:
          - mount
          - 'gcrypt:'
          - /data
          - --allow-other
          - --allow-non-empty
          - --gid=1000
          - --dir-cache-time=1000h
          - --poll-interval=10s
          - --user-agent=milton-plex/v1
          - --umask=002
          # - --use-mmap
          # - --vfs-read-chunk-size=10M
          # - --vfs-read-chunk-size-limit=0
          # - --buffer-size=0
          - --rc
          - --rc-addr=:5572
          - --rc-no-auth
          - --drive-pacer-min-sleep=10ms
          - --drive-pacer-burst=1000
          - --cache-dir=/cache
          - --vfs-cache-mode=full
          - --vfs-cache-max-size=460G
          - --vfs-cache-max-age=192h
          - --vfs-cache-poll-interval=5m
          - --vfs-read-ahead=1G
          - -v=1
        envFrom:
          - secretRef:
              name: rclone-secret
        securityContext:
          privileged: true
          capabilities:
            add:
              - SYS_ADMIN
        lifecycle:
          postStart:
            exec:
              command:
                - /bin/sh
                - -c
                - rclone rc vfs/refresh recursive=true --rc-addr 127.0.0.1:5572 _async=true
          preStop:
            exec:
              command:
                - /bin/sh
                - -c
                - fusermount -uz /data
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: 1000m
            memory: 1Gi
        volumeMounts:
          - name: cache
            mountPath: /cache
          - name: media-path
            mountPath: /data
            mountPropagation: Bidirectional
