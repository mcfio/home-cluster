---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: rclone-cache
  namespace: mediaserver
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 250Gi
  storageClassName: ceph-block
