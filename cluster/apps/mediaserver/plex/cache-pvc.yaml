---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: rclone-cache
  namespace: mediaserver
  labels:
    excluded_from_alerts: "true"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 300Gi
  storageClassName: ceph-block
