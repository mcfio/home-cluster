apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: home-shared-pvc
  namespace: home
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 30Gi
  storageClassName: ceph-filesystem
