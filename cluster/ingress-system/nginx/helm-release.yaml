---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ingress-nginx
  namespace: ingress-system
spec:
  interval: 12h
  maxHistory: 2
  chart:
    spec:
      chart: ingress-nginx
      version: 4.6.0
      sourceRef:
        kind: HelmRepository
        name: ingress-nginx
        namespace: ingress-system
  upgrade:
    cleanupOnFail: true
  uninstall:
    keepHistory: false
  values:
    controller:
      replicaCount: 3
      extraEnvs:
        - name: TZ
          value: Canada/Eastern
      service:
        enabled: true
        annotations:
          io.cilium/lb-ipam-ips: 192.168.50.100
      publishService:
        enabled: true
      ingressClassResource:
        default: true
      config:
        # client-header-timeout: 120
        # client-body-buffer-size: "100M"
        # client-body-timeout: 120
        # custom-http-errors: |-
        #   401,403,404,500,501,502,503
        # enable-brotli: "true"
        # forwarded-for-header: "CF-Connecting-IP"
        # hsts-max-age: "31449600"
        # keep-alive: 120
        # keep-alive-requests: 10000
        # proxy-body-size: "100M"
        ssl-protocols: "TLSv1.3 TLSv1.2"
        # use-forwarded-headers: "true"
      extraArgs:
        default-ssl-certificate: ingress-system/milton-mcf-io-wc-cert-tls
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: ingress-nginx
              app.kubernetes.io/component: controller
      resources:
        requests:
          cpu: 15m
          memory: 165M
        limits:
          memory: 270M
