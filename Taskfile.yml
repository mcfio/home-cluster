---
version: '3'

vars:
  PROJECT_DIR:
    sh: 'git rev-parse --show-toplevel'
  CLUSTER_DIR: '{{.PROJECT_DIR}}/cluster'

silent: true

includes:
  flux: .taskfiles/flux.yml
  ansible: .taskfiles/ansible.yml
  # rook-ceph: .taskfiles/rook-ceph.yml
  # scale: .taskfiles/scale.yml
