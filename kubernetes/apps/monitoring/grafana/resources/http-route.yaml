---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
spec:
  parentRefs:
    - name: internal
      namespace: ingress-system
      sectionName: https
  hostnames:
    - grafana.milton.mcf.io
  rules:
    - backendRefs:
        - name: grafana
          port: 80
